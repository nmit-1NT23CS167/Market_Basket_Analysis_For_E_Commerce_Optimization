# IntelliGrocery Environment Configuration Template
# Copy this to .env and update values as needed

# PostgreSQL Configuration (required)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=ravikiran
DB_NAME=intelligrocery_db

# pgAdmin4 Configuration (Docker)
PGADMIN_DEFAULT_EMAIL=admin@intelligrocery.local
PGADMIN_DEFAULT_PASSWORD=AdminPass@2024

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost

# Debug Mode (optional)
DEBUG=False
LOG_LEVEL=INFO
